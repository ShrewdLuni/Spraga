import { Layout } from "../../components/Layout"

interface ShopProps {

}

export const Shop = ({} : ShopProps) => {
  return (
    <Layout>
      <div className="w-screen h-screen">
        Shop page
      </div>
    </Layout>
  )
}